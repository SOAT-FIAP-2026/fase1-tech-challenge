{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "PLACEHOLDER — substituído por variável de ambiente em runtime"
  },
  "Jwt": {
    "Secret": "PLACEHOLDER — substituído por variável de ambiente em runtime",
    "Issuer": "PLACEHOLDER — substituído por variável de ambiente em runtime",
    "Audience": "PLACEHOLDER — substituído por variável de ambiente em runtime"
  }
}
